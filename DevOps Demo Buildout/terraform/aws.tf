provider "aws" {
# Using environment variables for Region & API Access Key
# not required if using environment variables on workstation
	#access_key = "${var.aws_access_key_id}"
	#secret_key = "${var.aws_secret_access_key}"
	#region = "${var.aws_region}"
}